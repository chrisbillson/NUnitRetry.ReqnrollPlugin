# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x
        
    - name: Restore src dependencies
      run: dotnet restore $GITHUB_WORKSPACE/src/NUnitRetry.sln
      
    - name: Restore test dependencies 
      run: dotnet restore $GITHUB_WORKSPACE/tests/NUnitRetryTests.sln
      
    - name: Build src
      run: dotnet build $GITHUB_WORKSPACE/src/NUnitRetry.sln --no-restore  --configuration release
      
    - name: Build test
      run: dotnet build $GITHUB_WORKSPACE/tests/NUnitRetryTests.sln --no-restore
    
